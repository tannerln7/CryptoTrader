# Local Data Placeholder

This directory exists only as a local placeholder for data mounts or repo-local experimentation.

Do not commit real market data. The `.gitignore` intentionally ignores runtime data while preserving this placeholder.