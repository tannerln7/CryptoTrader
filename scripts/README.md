# Scripts

Use this directory for developer-facing helper entrypoints that do not belong inside the runtime package.

Examples include validation helpers, one-off migration utilities, and operational maintenance scripts.