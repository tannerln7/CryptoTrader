# Integration Tests

Use this directory for multi-module, transport, file-system, or provider smoke tests once recorder behavior exists.

Phase 0 adds the directory only so future work does not need to invent placement.